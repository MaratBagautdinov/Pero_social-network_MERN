import s from './DialogsPage.module.sass'

const DialogsPage = () => {
    return (
        <div className={s.DialogsPage}>
            <h2>Dialogs</h2>
        </div>
    );
};

export default DialogsPage;