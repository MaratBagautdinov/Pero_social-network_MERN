import s from './ProfilePage.module.sass'

const ProfilePage = () => {
    return (
        <div className={s.ProfilePage}>
            <h2>Profile</h2>
        </div>
    );
};

export default ProfilePage;