import s from './SettingsPage.module.sass'

const SettingsPage = () => {
    return (
        <div className={s.SettingsPage}>
            <h2>Settings</h2>
        </div>
    );
};

export default SettingsPage;